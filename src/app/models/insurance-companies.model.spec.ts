import { InsuranceCompanies } from './insurance-companies.model';

describe('InsuranceCompanies', () => {
  it('should create an instance', () => {
    expect(new InsuranceCompanies()).toBeTruthy();
  });
});
